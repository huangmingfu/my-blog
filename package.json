{
  "name": "vitepress-blog",
  "version": "1.0.0",
  "private": true,
  "author": "a无名",
  "license": "MIT",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs",
    "deploy": "pnpm run build &&  git add . && git commit -m \"gitee 自动推送\" && git subtree push --prefix docs/.vitepress/dist origin gh-pages && rimraf docs/.vitepress/dist &&  git add . && git commit -m \"gitee 自动推送\" && git push -u origin master "
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.1",
    "@sugarat/theme": "^0.2.24",
    "element-plus": "^2.5.3",
    "vue": "3.4.15"
  },
  "directories": {
    "doc": "docs"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "sass": "^1.64.1",
    "typescript": "^5.1.6",
    "vitepress": "1.0.0-rc.45"
  }
}
